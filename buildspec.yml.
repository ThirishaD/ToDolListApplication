version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17  # Update Java version if needed
    commands:
      - echo Installing dependencies...
      - mvn install

  build:
    commands:
      - echo Building application...
      - mvn package

  post_build:
    commands:
      - echo Build completed successfully
